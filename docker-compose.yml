services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sport_agent_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - OTEL_SDK_DISABLED=true
      - CREWAI_TELEMETRY_OPT_OUT=true
    restart: unless-stopped
